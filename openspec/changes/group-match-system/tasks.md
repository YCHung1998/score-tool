## 1. 狀態模型與持久化

- [x] 1.1 定義新 `state` 結構：`players[]`, `groups[]`, `matches[]`, `nextPlayerId`, `nextGroupId`, `nextMatchId`
- [x] 1.2 更新 `saveState()` 與 `loadState()` 使用新 storage key `group_match_v1`
- [x] 1.3 `loadState()` 偵測舊 `score_tracker_v1` key，若存在則忽略並提示格式不同

## 2. 名單面板（選手 + 小組）

- [x] 2.1 保留現有「新增 / 刪除選手」UI（`#panel-roster` 上半部）
- [x] 2.2 新增「建立小組」區塊：輸入小組名稱 → 勾選成員 → 建立
- [x] 2.3 禁止重複小組名稱，顯示錯誤提示
- [x] 2.4 小組列表顯示：名稱 + 成員姓名列表 + 解散按鈕
- [x] 2.5 有場次的小組不允許刪除（顯示提示）

## 3. 賽程面板（新 tab）

- [x] 3.1 將原「計分」tab 改為「賽程」tab，切換 tab id 為 `schedule`，更新 `TAB_TITLES`
- [x] 3.2 「新增場次」卡片：
  - [x] 3.2a 輸入輪次標籤（可空）
  - [x] 3.2b Side 1 小組多選（checkbox 清單）
  - [x] 3.2c Side 2 小組多選（checkbox 清單）
  - [x] 3.2d 確認新增，驗證 side1 與 side2 各至少 1 組且互不重疊
- [x] 3.3 場次列表：顯示輪次標籤、兩側隊伍組成、計分狀態（待計分 / 已計分+比數）
- [x] 3.4 場次可刪除（尚未計分的場次）

## 4. 計分面板

- [x] 4.1 將原「計分」面板內容改為「場次計分」：列出所有場次（含待計分）
- [x] 4.2 每個場次顯示兩側隊名，兩個數字輸入框，「儲存」按鈕
- [x] 4.3 輸入驗證：兩側分數均為非負整數才允許儲存
- [x] 4.4 已計分場次顯示目前比數，允許修改後重新儲存
- [x] 4.5 儲存後即時更新排名

## 5. 排名引擎

- [x] 5.1 實作 `computeGroupStandings()` 函式：
  - [x] 5.1a 遍歷所有已計分 matches，判斷每個 group 的勝/負/平
  - [x] 5.1b 累計 wins, losses, diff（僅勝場加差分）
  - [x] 5.1c 排序：wins DESC → diff DESC → name ASC

## 6. 排名面板

- [x] 6.1 排名表：名次、小組名稱、成員（小字）、勝場、負場、差分
- [x] 6.2 場次結果清單（取代「各場紀錄」）：
  - [x] 6.2a 依輪次標籤分組顯示
  - [x] 6.2b 每場顯示兩側隊名、比數、勝者（或「待計分」）
- [x] 6.3 尚無場次資料時顯示引導空狀態

## 7. 匯出 / 匯入

- [x] 7.1 實作新 `exportTxt()` 寫出 `GROUP_MATCH_V1` 格式
- [x] 7.2 實作新 `importTxt()` 解析 `GROUP_MATCH_V1` 格式
- [x] 7.3 匯入時偵測舊 `SCORE_TRACKER_V1` 格式，顯示「不相容舊格式」提示

## 8. 說明 tab 更新

- [x] 8.1 更新「說明」面板內容，反映新操作流程：建立名單 → 建立小組 → 建立賽程 → 計分 → 看排名

## 9. 手動驗證

- [x] 9.1 建立 ABCDX 五組，按範例賽制新增 9 場場次，驗證賽程顯示正確
- [x] 9.2 逐一輸入分數，驗證排名即時更新
- [x] 9.3 測試匯出 → 清除 → 匯入流程，資料完整復原
