## 1. 新增 CSS 樣式

- [x] 1.1 在 `<style>` 新增 `.nav-label { display: block; font-size: 10px; }`
- [x] 1.2 在 `<style>` 新增 `.nav-sublabel { display: block; font-size: 8px; color: #86868b; line-height: 1.1; margin-top: 1px; }`

## 2. 更新導覽按鈕文字

- [x] 2.1 名單 按鈕：將純文字 `名單` 替換為 `<span class="nav-label">名單</span><span class="nav-sublabel">Roster</span>`
- [x] 2.2 賽程 按鈕：將純文字 `賽程` 替換為 `<span class="nav-label">賽程</span><span class="nav-sublabel">Schedule</span>`
- [x] 2.3 計分 按鈕：將純文字 `計分` 替換為 `<span class="nav-label">計分</span><span class="nav-sublabel">Score</span>`
- [x] 2.4 排名 按鈕：將純文字 `排名` 替換為 `<span class="nav-label">戰績</span><span class="nav-sublabel">Battle Record</span>`
- [x] 2.5 紀錄 按鈕：將純文字 `紀錄` 替換為 `<span class="nav-label">紀錄</span><span class="nav-sublabel">Records</span>`
- [x] 2.6 說明 按鈕：將純文字 `說明` 替換為 `<span class="nav-label">說明</span><span class="nav-sublabel">Guide</span>`

## 3. 更新 JS 頁面標題

- [x] 3.1 將 `TAB_TITLES.rankings` 的值從 `'排名'` 改為 `'戰績'`

## 4. 更新說明頁 wireframe mockup

- [x] 4.1 將排名 mockup 的 `<div class="tp-header">排名</div>` 改為 `<div class="tp-header">戰績</div>`
- [x] 4.2 將排名 mockup 的 `<p class="tp-label">📊 排名</p>` 改為 `<p class="tp-label">📊 戰績</p>`

## 5. 手動驗證

- [ ] 5.1 確認所有 6 個 nav 按鈕顯示中英雙標籤
- [ ] 5.2 點擊戰績 tab，確認頁面標題顯示「戰績」
- [ ] 5.3 確認說明頁 wireframe 第四個 mockup 顯示「戰績」
- [ ] 5.4 確認其他功能正常（排名計算、計分、賽程不受影響）
